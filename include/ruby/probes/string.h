#define PROBE_STRING_ENTRY(probe,str) PROBE_ENTRY(probe,(char*)rb_sourcefile(),(int)rb_sourceline(),(char*)RSTRING_PTR(str))
#define PROBE_STRING_INT_ENTRY(probe,str,len) PROBE_ENTRY(probe,(char*)rb_sourcefile(),(int)rb_sourceline(),(char*)RSTRING_PTR(str),(long)(len))
#define PROBE_STRING1_ENTRY(probe,str,str2) PROBE_ENTRY(probe,(char*)rb_sourcefile(),(int)rb_sourceline(),(char*)RSTRING_PTR(str),(char*)RSTRING_PTR(str2))
#define PROBE_STRING_RETURN(probe,str) PROBE_RETURN(probe,(char*)rb_sourcefile(),(int)rb_sourceline(),(char*)RSTRING_PTR(str))
#define PROBE_STRING_INT_RETURN(probe,str,len) PROBE_RETURN(probe,(char*)rb_sourcefile(),(int)rb_sourceline(),(char*)RSTRING_PTR(str),(long)(len))
#define PROBE_STRING1_RETURN(probe,str,str2) PROBE_RETURN(probe,(char*)rb_sourcefile(),(int)rb_sourceline(),(char*)RSTRING_PTR(str),(char*)RSTRING_PTR(str2))

#define PROBE_STR_NEW_ENTRY(ptr,len) PROBE_ENTRY(STR_NEW,(char*)rb_sourcefile(),(int)rb_sourceline(),(void*)ptr,(int)(len))
#define PROBE_STR_NEW_RETURN(ptr,len) PROBE_RETURN(STR_NEW,(char*)rb_sourcefile(),(int)rb_sourceline(),(void*)ptr,(int)(len))
#define PROBE_STR_NEW_SHARED_ENTRY(str) PROBE_STRING_ENTRY(STR_NEW_SHARED,str)
#define PROBE_STR_NEW_SHARED_RETURN(str) PROBE_STRING_RETURN(STR_NEW_SHARED,str)
#define PROBE_STR_FREE_ENTRY(str) PROBE_STRING_ENTRY(STR_FREE,str)
#define PROBE_STR_FREE_RETURN(str) PROBE_STRING_RETURN(STR_FREE,str)
#define PROBE_STR_DUP_ENTRY(str) PROBE_STRING_ENTRY(STR_DUP,str)
#define PROBE_STR_DUP_RETURN(str) PROBE_STRING_RETURN(STR_DUP,str)
#define PROBE_STR_REPLACE_ENTRY(str,str2) PROBE_STRING1_ENTRY(STR_REPLACE,str,str2)
#define PROBE_STR_REPLACE_RETURN(str,str2) PROBE_STRING1_RETURN(STR_REPLACE,str,str2)
#define PROBE_STR_SHARED_REPLACE_ENTRY(str,str2) PROBE_STRING1_ENTRY(STR_SHARED_REPLACE,str,str2)
#define PROBE_STR_SHARED_REPLACE_RETURN(str,str2) PROBE_STRING1_RETURN(STR_SHARED_REPLACE,str,str2)
#define PROBE_STR_BUF_NEW_ENTRY(size) PROBE_ENTRY(STR_BUF_NEW,(char*)rb_sourcefile(),(int)rb_sourceline(),(int)size)
#define PROBE_STR_BUF_NEW_RETURN(size) PROBE_RETURN(STR_BUF_NEW,(char*)rb_sourcefile(),(int)rb_sourceline(),(int)size)
#define PROBE_STR_BUF_CSTR_NEW_ENTRY(ptr) PROBE_ENTRY(STR_BUF_CSTR_NEW,(char*)rb_sourcefile(),(int)rb_sourceline(),(void*)ptr)
#define PROBE_STR_BUF_CSTR_NEW_RETURN(ptr) PROBE_RETURN(STR_BUF_CSTR_NEW,(char*)rb_sourcefile(),(int)rb_sourceline(),(void*)ptr)
#define PROBE_STR_RESIZE_CAPACITY_ENTRY(str,cap) PROBE_STRING_INT_ENTRY(STR_RESIZE_CAPACITY,str,cap)
#define PROBE_STR_RESIZE_CAPACITY_RETURN(str,cap) PROBE_STRING_INT_RETURN(STR_RESIZE_CAPACITY,str,cap)
#define PROBE_STR_DEC_LENGTH_ENTRY(str) PROBE_STRING_ENTRY(STR_DEC_LENGTH,str)
#define PROBE_STR_DEC_LENGTH_RETURN(str) PROBE_STRING_RETURN(STR_DEC_LENGTH,str)
#define PROBE_STR_SET_LENGTH_ENTRY(str,len) PROBE_STRING_INT_ENTRY(STR_SET_LENGTH,str,len)
#define PROBE_STR_SET_LENGTH_RETURN(str,len) PROBE_STRING_INT_RETURN(STR_SET_LENGTH,str,len)